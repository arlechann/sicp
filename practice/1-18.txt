(define (even? x) (= (remainder n 2) 0))
(define (double x) (* x 2))
(define (halve x) (/ x 2))

(define (* a b)
  (define (iter a b sum)
    (cond ((= b 0) 1)
          ((even? b) (iter (double a) (halve b) sum))
          (else (iter a (- b 1) (+ sum a)))))
  (iter a b 0))
